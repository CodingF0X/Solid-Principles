package open_closed;
public class DrawShape {

    // This one dont care about the logic of drawing each shape
    // instead, it gets the shape drawn, it only prints it.
    public void draw_Shape (Ishape shape){
     shape.draw();
    }
}
